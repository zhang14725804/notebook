<template name="img-loader">
    <image binderror="_imgOnLoadError" bindload="_imgOnLoad" data-src="{{item}}" src="{{item}}" style="width:0;height:0;opacity:0" wx:if="{{imgLoadList}}" wx:for="{{imgLoadList}}" wx:key="{{ `imgLoadItem${index}` }}"></image>
</template>
