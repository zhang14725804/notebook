<template name="banner">
    <view class="banner {{isUserCenter?'banner--usercenter':''}}">
        <image class="banner__logo" src="{{logo}}"></image>
        <view class="banner__title" wx:if="{{title}}">{{title}}</view>
    </view>
</template>
