"use strict";

var constants = require("p_./components/showcase/feature/components/goods/constants.wxs")();

function getConerMarkClass(type) {
    var className = "sc-goods__corner-mark";
    className += " sc-goods__corner-mark--" + type;
    className += " zan-icon zan-icon-" + constants.cornerMarkMap[(nt_0 = type.toString(), 
    null == nt_0 ? undefined : "Number" === nt_0.constructor ? nt_0 : "" + nt_0)];
    return className;
}

function getConerBackClass(type) {
    var className = "sc-goods__corner-back";
    className += " sc-goods__corner-back--" + type;
    return className;
}

module.exports = {
    getConerMarkClass: getConerMarkClass,
    getConerBackClass: getConerBackClass
};