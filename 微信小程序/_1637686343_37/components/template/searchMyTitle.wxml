<template name="m-search-title">
    <view class="m-title">
        <text class="m-title_h">{{title}}</text>
        <form bindsubmit="clearSearchHistory" class="m-title_clear" reportSubmit="true">
            <button class=" m-title_clearTx button-in-form" data-bindtap="clearSearchHistory" formType="submit" wx:if="{{showclear}}">
            清空历史
          </button>
        </form>
    </view>
</template>
